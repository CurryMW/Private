# 必填：模型服务凭证。真实值只能保存在 .env 或 GitHub Secrets 中。
AI_API_KEY=
# TeamoRouter OpenAI-compatible 接口地址与唯一生产模型。
AI_BASE_URL=https://api.teamorouter.cn/v1
AI_MODEL=gpt-5.6-luna

# 必填：百度官方搜索 API 凭证，与模型服务凭证分开。
BAIDU_SEARCH_API_KEY=

# 必填：钉钉机器人的 HTTPS Webhook，可以已经包含 access_token。
DINGTALK_WEBHOOK=
# 如果 DINGTALK_WEBHOOK 是包含 access_token 的完整 URL，此项可以留空。
DINGTALK_ACCESS_TOKEN=

WINDOW_HOURS=36
MAX_ITEMS=8
TIMEZONE=Asia/Shanghai
# 安全的本地默认值：只预演，不发送钉钉消息，也不修改已发送状态。
DRY_RUN=true
STATE_PATH=.state/sent.json
BAIDU_USAGE_STATE_PATH=.state/baidu-search-usage.json

# GitHub 趋势独立配置；Actions 自动提供只读 Job Token。
GITHUB_TOKEN=
GITHUB_TRENDS_STATE_PATH=.state/github-trends/baseline.json
