# 05 — 切换独立的 AI 情报工作流

**What to build:** 将每日 AI 情报摘要切换到百度搜索与已验证的 TeamoRouter `gpt-5.6-luna` 组成的新链路，并以独立工作流在北京时间 08:30 运行。迁移既有 URL 成功状态，落实“仅成功后写状态”和失败静默规则，先完成手工 dry-run 验收再启用定时发送。
**Blocked by:** 03 — 完成证据驱动的 AI 情报摘要; 04 — 验证并接入唯一生产模型.
**Status:** integrated — `8c6c5fc`；2026-09-20 修复版真实 dry-run 返回 `status=empty`

- [x] AI 情报摘要工作流每天北京时间 08:30 触发，且可以手工执行 dry-run。
- [x] 工作流分别读取百度搜索凭据、TeamoRouter 模型凭据和钉钉凭据，日志与构建产物不暴露其值。
- [x] 手工 dry-run 展示最终预览，但不发送钉钉消息、不写成功状态。
- [x] 真实发送仅在搜索、筛选、模型生成、消息校验和钉钉发送全部成功后记录成功状态。
- [x] 搜索或模型失败会令工作流失败，钉钉群保持静默，不发送错误、降级或运行状态通知。
- [x] 没有合格内容时工作流成功结束但不发送钉钉消息，也不写入本次发送成功状态。
- [x] 百度免费额度耗尽或限额响应会使工作流失败，不自动启用后付费或其他搜索来源。
- [x] 既有 URL 去重历史被迁移并继续生效，迁移失败不得悄悄清空历史后发送重复内容。
- [x] 在启用定时发送前，使用真实配置完成一次人工 dry-run；本次返回无合格候选，未发送钉钉且隔离状态目录为空。
